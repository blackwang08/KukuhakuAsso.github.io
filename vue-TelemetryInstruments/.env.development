VITE_API_SCF_BASE=/api-scf
VITE_HOME_URL=http://localhost:5173

# 线上 CVM 默认目标（提交到 git）
# 如需切到本地后端，请在 .env.development.local 中覆盖以下变量（已 gitignore）
# 本地后端通常需要 API_SCF_REWRITE=true 来去掉 /api-scf 前缀
API_SCF_TARGET=https://www.ku2hakuasso.site
API_SCF_REWRITE=false
