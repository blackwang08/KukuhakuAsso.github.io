# /api-mist 前缀（默认，与 projects.json 的 proxyApi 对齐）
VITE_API_MIST_BASE=/api-mist

# 后端 API 目标（本地后端 / 线上 CVM 均可）
# REWRITE=true：转发时去掉 /api-mist 前缀（本地直连后端用）
API_SCF_TARGET=https://www.ku2hakuasso.site
API_SCF_REWRITE=true
